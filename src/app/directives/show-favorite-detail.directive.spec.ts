import { ShowFavoriteDetailDirective } from './show-favorite-detail.directive';

describe('ShowFavoriteDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFavoriteDetailDirective();
    expect(directive).toBeTruthy();
  });
});
