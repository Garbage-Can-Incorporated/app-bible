import { DeleteFavoriteDirective } from './delete-favorite.directive';

describe('DeleteFavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
