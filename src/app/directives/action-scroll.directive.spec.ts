import { ActionScrollDirective } from './action-scroll.directive';

describe('ActionScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionScrollDirective();
    expect(directive).toBeTruthy();
  });
});
