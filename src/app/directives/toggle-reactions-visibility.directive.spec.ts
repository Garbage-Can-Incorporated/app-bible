import { ToggleReactionsVisibilityDirective } from './toggle-reactions-visibility.directive';

describe('ToggleReactionsVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleReactionsVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
