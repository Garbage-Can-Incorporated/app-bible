import { ScriptureNavigatorDirective } from './scripture-navigator.directive';
import { ElementRef } from '@angular/core';

describe('ScriptureNavigatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScriptureNavigatorDirective();
    expect(directive).toBeTruthy();
  });
});
