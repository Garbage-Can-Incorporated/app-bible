import { FormatVersePipe } from './format-verse.pipe';

describe('FormatVersePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatVersePipe();
    expect(pipe).toBeTruthy();
  });
});
