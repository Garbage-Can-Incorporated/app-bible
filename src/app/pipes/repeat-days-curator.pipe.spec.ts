import { RepeatDaysCuratorPipe } from './repeat-days-curator.pipe';

describe('RepeatDaysCuratorPipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatDaysCuratorPipe();
    expect(pipe).toBeTruthy();
  });
});
